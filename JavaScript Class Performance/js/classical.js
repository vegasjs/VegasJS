function Person (name) {
  this.name = name;

  this.setGender = function (gender) {
    this.gender = gender;
  };
  
  this.sayHello = function(noun) {
    this.message = "Hello, my name is " + this.name + " and I'm a " + noun;
  };
  
  this.setGender = function (gender) {
    this.gender = gender;
  };
  
  this.sayHello = function(noun) {
    this.message = "Hello, my name is " + this.name + " and I'm a " + noun;
  };
  
  this.setGender = function (gender) {
    this.gender = gender;
  };
  
  this.sayHello = function(noun) {
    this.message = "Hello, my name is " + this.name + " and I'm a " + noun;
  };
  
  this.setGender = function (gender) {
    this.gender = gender;
  };
  
  this.sayHello = function(noun) {
    this.message = "Hello, my name is " + this.name + " and I'm a " + noun;
  };
}